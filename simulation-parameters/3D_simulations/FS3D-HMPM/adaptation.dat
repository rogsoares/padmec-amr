Set error analisys and mesh adaptation parameters


Use mesh adaptation:
no

Refinement Strategy: {h-refinement, remeshing, rh-refinement}
remeshing

Error tolerance for all mesh elements:
0.3

Obs.: If any mesh element is flagged as singular, the value of the following parameter will be ineffective no matter it is.
Error tolerance for all mesh elements excluding those on singularities regions:
0.2

Obs.: For h-refinement
Maximum number of 2D element subdivisions (recommended: 4)
3

Obs.: For h-refinement
Maximum number of 3D element subdivisions (recommended: 3)
3

Obs.: For h-refinement
Maximum number of refinement steps:
12

Obs.: Error tolerance for all mesh elements must be greater than the tolerance excluding those on singularities regions:
      Singularities are critical because error are high than in other domain's parts.
      
Interpolation methods:
() - h_REFINEMENT
(x) - LINEAR
() - QUADRATIC
() - ADAPTATIVE
() - CONSERVATIVE
() - PURE_INJECTION
() - HALF_WEIGHTING
() - FULL_WEIGHTING

Obs.: For h-refinement
Percentages of element height. The first one is for refinement and the second for unrefinement.
Parameters to decide if an element must be (un)refined or not.
1
1

Obs.: For remeshing
Set n to define a minimum allowed element heigth for remesh adaptation.
h_min_allowed = h_min/n, where h_min is the minimum element height of the initial mesh.
5
